.build/
.swiftpm/
*.xcodeproj/
DerivedData/
build/
*.app
*.tar.gz
.env
.secrets/
.DS_Store

# IDE
# Allow the shared launch.json so contributors get a working Swift debug
# config out of the box; ignore everything else under .vscode/ (per-user
# state like .vscode/settings.json that varies by machine).
.vscode/*
!.vscode/launch.json
.idea/

# Code signing artifacts — credentials, never commit
*.p12
*.cer
*.p8
